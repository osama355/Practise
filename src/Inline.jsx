import React from "react";

const Inline = () => {
  const heading = {
    fontSize: "72px",
    color: "red"
  };

  return (
    <div>
      <h1 style={heading}>Inline CSS</h1>
    </div>
  );
};

export default Inline;
